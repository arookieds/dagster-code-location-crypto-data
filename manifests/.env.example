# Example environment variables for sealed secrets generation
# Copy this file to .env and fill in actual values
# DO NOT commit .env to Git!

# Database credentials (matches Settings.db_username, Settings.db_password)
DB_USERNAME=crypto_user
DB_PASSWORD=your-secure-password-here

# S3/MinIO credentials (matches Settings.s3_user, Settings.s3_password)
S3_USER=your-minio-access-key
S3_PASSWORD=your-minio-secret-key
